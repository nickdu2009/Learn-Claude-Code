You must use the `load_skill` tool before answering.

Load the `pdf` skill first, then answer using that loaded skill:

1. What exact command name does the skill recommend for quick PDF text extraction?
2. What exact command name does the skill recommend for creating a PDF from Markdown?

Keep the answer short and mention the command names explicitly.
