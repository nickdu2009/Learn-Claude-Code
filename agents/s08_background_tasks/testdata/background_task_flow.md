Start the shell command `sleep 1; echo background finished` with `background_run`.
While it runs, use `write_file` to create `config.txt` with the exact content `configured`.
After the background command finishes, summarize both outcomes in one short reply.
